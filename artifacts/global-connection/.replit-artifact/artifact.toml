kind = "web"
previewPath = "/"
title = "Global Connection Travels & Tours"
version = "1.0.0"
id = "artifacts/global-connection"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22649

[services.development]
run = "pnpm --filter @workspace/global-connection run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/global-connection", "run", "build" ]
publicDir = "artifacts/global-connection/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22649"
BASE_PATH = "/"
